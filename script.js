function GoToSectionRecommendation() {
    var elementos = document.getElementsByClassName("recommendations");
    if (elementos.length > 0) {
      elementos[0].scrollIntoView({ behavior: "smooth" });
    }
  }
  function GoToSectionTourist() {
    var elementos = document.getElementsByClassName("tourist-cities");
    if (elementos.length > 0) {
      elementos[0].scrollIntoView({ behavior: "smooth" });
    }
  }

